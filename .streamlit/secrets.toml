# Streamlit Secrets Configuration
# Sostituisci i valori placeholder con le tue credenziali reali.

# Token GitHub (Personal Access Token con permessi repo) per il backup automatico dei progetti
GITHUB_TOKEN = "YOUR_GITHUB_TOKEN"

# Credenziali Google Service Account in formato JSON (su una sola riga o multiriga)
# per il backup automatico su Google Drive
GOOGLE_CREDENTIALS_JSON = ""
